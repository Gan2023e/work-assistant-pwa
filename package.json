{
  "name": "work-assistant-pwa",
  "version": "2.0.5",
  "main": "backend/app.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "description": "工作助手PWA全栈应用",
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
